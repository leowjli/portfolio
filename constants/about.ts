/**
 * Animation and layout constants for the About page
 */

// Scene transition overlap size (0-1 range)
export const SCENE_OVERLAP_SIZE = 0.1;

// Directional slide distances (in pixels)
export const SLIDE_DISTANCE = 12;

// Stage scroll height multiplier
export const STAGE_HEIGHT_VH = 400;

// First scene visibility threshold
export const FIRST_SCENE_THRESHOLD = 0.05;
